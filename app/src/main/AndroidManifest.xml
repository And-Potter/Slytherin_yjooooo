<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yjooooo.sopt28th">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.28thSOPT">
        <activity android:name=".ui.home.view.HomeActivity"></activity>
        <activity android:name=".ui.userinfo.UserInfoActivity"></activity>
        <activity android:name=".ui.signup.SignUpActivity" />
        <activity android:name=".ui.signin.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>